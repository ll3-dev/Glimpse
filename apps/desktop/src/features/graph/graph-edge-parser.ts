/**
 * Edge parsing lives in @glimpse/features so mobile AI recommendations and
 * the desktop graph accept the same LLM response format.
 */
export { parseEdges, type ProposedEdge } from '@glimpse/features';
