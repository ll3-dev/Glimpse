export async function initializeCoreClient(): Promise<string | null> {
  return null;
}

/**
 * Returns the current database path (web: always null).
 */
export async function getCoreDbPath(): Promise<string | null> {
  return null;
}
